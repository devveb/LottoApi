server:
    port: 28081
    tomcat:
        uri-encoding: UTF-8

spring:
    profiles:
        active: awsdep

mybatis:
    mapper-locations: classpath*:sqlmapper/*.xml
    type-aliases-package: com.sbsft.wslapi.model
    configuration:
        map-underscore-to-camel-case: true
        default-fetch-size: 20
        default-statement-timeout: 30